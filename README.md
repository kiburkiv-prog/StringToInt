# StringToInt
A simple string to integer converter for C.
# Usage
Put to_int.h into your project's directory and include using #include "to_int.h".
Put the string (character array) as the first parametr and the size of the string (can use sizeof(your_string)) as the second one.
